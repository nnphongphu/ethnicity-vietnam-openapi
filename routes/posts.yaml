get:
  tags:
    - Posts
  summary: Get latest posts
  description: Return posts sorted by creation date in descending order
  parameters:
    - name: cursorId
      in: query
      description: >-
        ID for pagination to get posts created before the cursor
        (if not specified, will return latest posts)
      schema:
        type: string
    - name: size
      in: query
      description: The number of posts to return (defaults to 10)
      schema:
        type: integer
        mininum: 1
        maximum: 10
  responses:
    '200':
      description: Successfully get post
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/Post.yaml'
    '500':
      description: Internal server error
post: 
  tags:
    - Posts
  summary: Update an existing post
  description: Update an existing post
  requestBody:
    description: The content to update
    required: true
    content:
      application/json:
        schema:
          allOf:
            - $ref: '../schemas/Post.yaml'
            - required:
              - title
              - thumbnailUrl
              - preview
              - body
              - status
              - category
              - createdAt
              - updatedAt
  responses:
    '200':
      description: Successfully create post
      content:
        application/json:
          schema:
            type: object
            properties:
              id:
                type: string
                description: ID of the newly created post
    '400':
      description: Bad request
    '401':
      description: Unauthorized
    '500':
      description: Internal server error
  security:
    - api_key_authentication: []